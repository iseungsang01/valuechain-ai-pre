# Task: Python Calculator TDD
Write a Python calculator class in `calc.py` with `add` and `subtract` methods. 
Write tests for it in `test_calc.py` using standard `assert` statements.

test_command: "python test_calc.py"
